# Editable art sources

Store original layered masters here using lowercase snake_case names. PSD, KRA, BLEND, ASEPRITE, and XCF files are Git LFS objects. Each production asset must be represented in art/provenance.json.

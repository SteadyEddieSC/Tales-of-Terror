# v0.1.6 Tale Library Evidence Ledger

## Candidate identity

- Required base: `0dde8d41c7cb1fc23bb2d85c26cb9281e311c971`
- Branch: `agent/issue-45-tale-library-selection-ux`
- Candidate head: recorded after the final evidence-only commit
- Draft PR: recorded after publication
- Catalog SHA-256: `2b478fd0d11fa075c2050409193aa06e6b9ca4dcf6efd4e4c550a9f3a5ff9db6`
- Tale package SHA-256: `abb39d6bfbdf8d7de108379f08180c13efb99bbffa3e53f30eaaa8de7f459dee`
- Production inventory: one Tale, `lantern_house_vertical_slice` / Lantern House

## Route and state transitions

| From | Input | To | Preserved/committed state |
| --- | --- | --- | --- |
| Seat Lobby | owned A/Enter or Space | Mode Confirmation | Stable roster and ownership |
| Mode Confirmation | A/Enter or Space | Tale Library | Roster, ownership, selected mode, default selection |
| Tale Library | D-pad/stick/arrows/WASD | Tale Library | Focus only; no authority/RNG mutation |
| Tale Library | A/Enter | Public Briefing | Complete validated selection and prepared candidate atomically |
| Tale Library | B/Escape | Mode Confirmation | Roster, ownership, mode, selection |
| Public Briefing | B/Escape | Tale Library | Focus, selected/confirmed Tale, roster, ownership, mode |
| Public Briefing | A/Enter or Space | Active Tale | Selection locks for the session |

## Focused validation matrix

| Class | Expected evidence |
| --- | --- |
| Production rendering | One entry and truthful singular count from governed metadata |
| Controller/keyboard | D-pad, left stick, arrow/WASD focus; A/Enter confirm; B/Escape back; X/H Help |
| Restoration | Briefing back retains focus, selection, seats, ownership, and mode |
| Synthetic ordering | Two sorted export-excluded entries; deterministic wrap and non-default selection |
| Invalid identity/content | Catalog digest, unknown Tale, provider, package, localization, and incomplete display reject |
| Atomic failure | Prior selection, route, seats, mode, snapshot, and RNG state remain unchanged |
| Session lock | Active-session selection rejects without mutation |
| Privacy | No path/provider/hash/class/source-ledger data in UI, snapshot, report, public/Companion view |
| Lifecycle | 1–8 seats, no-phone, reset, rematch, return-to-title, replay/RNG invariance |

## Local validation

- Official Godot 4.7.1-stable typed editor import and three-second main-scene smoke: pass.
- Tale Library focused suite: 84 assertions pass, including governed production rendering,
  synthetic two-entry focus/selection, all bounded rejection classes, atomic retention,
  session immutability, privacy, no-phone play, and 1-8 stable seats.
- Controller-first main-route/report integration: 102 assertions pass, including D-pad,
  left-stick, keyboard, confirm, back, Help, protected reset, route ordering, restoration,
  and bounded guidance.
- Inherited deterministic suites: Director 90/90; social 157/157; Companion 40/40;
  vertical slice 24/24; golden replay equivalence 7/7; all other standalone board,
  rules, card, role, visual-language, package, catalog, route, pilot, and portable suites
  pass.
- GUT/JUnit: 5 scripts, 23 tests, and 113 assertions pass. An unmistakably temporary
  probe produced 1 intentional failure in 24 tests with a nonzero process exit and
  JUnit `failures=1`; the probe was removed and the unmodified 23-test suite passed
  again with JUnit `failures=0`.
- First-party static quality: all 95 first-party GDScript files pass gdlint with zero
  findings and gdformat `--check` with zero differences. Vendored GUT remains excluded
  and unchanged.
- Python 3.11.9 hash-locked install, `pip check`, repository privacy/provenance,
  no-network, secret, title, JSON/YAML, size, LFS, whitespace, toolchain, Tale package,
  Tale catalog, portable, and pilot validators: pass.
- Companion functional validation: Node 24.18.0 / npm 11.16.0; clean `npm ci`, exact
  dependency graph, strict TypeScript, service 26/26, browser 10/10, Worker dry-run,
  browser production build, and local browser-service-native-browser exactly-once ACK
  all pass. The separately classified audit-only deviation is recorded below.
- No gameplay, Tale package/catalog, report, snapshot, RNG, Companion protocol, package,
  lockfile, web/Companion source, room-service source, toolchain, vendored GUT, or action
  pin change is present.

Exact-head workflow runs and independently verified portable artifacts are added after
the draft candidate is published. Automated/headless execution is not human or
physical-device evidence.

## Issue #44 deviation

The Companion dependency files and sources are unchanged. The separately executed
`npm audit --audit-level=high` exits 1 and reports only GHSA-f88m-g3jw-g9cj through
Wrangler 4.110.0 -> Miniflare 4.20260708.1 -> Sharp 0.34.5: three High and zero
Critical findings. This is the exact issue #44 audit-only deviation. TypeScript,
service/browser tests, builds, local service smoke, and browser-service-native-browser
exactly-once acknowledgement pass separately. No suppression, override, forced
resolution, exception, deployment, or dependency change was used.

## Warnings and retries

- `npm ci` emitted npm's reviewed build-script allow-list warning for esbuild, Sharp,
  and workerd; installation and every functional Companion check completed successfully.
- A local exploratory JSON scan encountered a BOM in an ignored historical workspace
  output. The authoritative tracked-file scan then validated all 26 tracked JSON and
  seven tracked YAML files; no tracked repository defect existed.
- One first-party lint command was initially supplied an over-broad Windows path glob
  and therefore inspected the excluded vendored GUT tree. The corrected repository
  boundary checked exactly 95 first-party files with zero findings/differences; no
  vendored file was changed.
- The Tale package regression suite found and prevented a pre-commit retained-selection
  entry bug during implementation. The atomic `prepare_entry` boundary was corrected,
  and the Tale package, Tale Library, replay, vertical-slice, and route suites all passed
  afterward.

## Manual classifications

- Human pilot: not tested
- Physical controller: not tested
- Native TV/viewing distance: not tested
- Remote device: not tested
- Windows native/launcher execution: automated/headless when technically executed
- Linux native/launcher execution: automated/headless CI

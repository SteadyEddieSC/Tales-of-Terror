# v0.1.4 Tale Package & Authoring Validator Evidence Ledger

## Fixed boundary

- Starting protected main: `f455f6a0e723ca868b46c11e885c54fd250a4d43`.
- Branch: `agent/issue-40-tale-package-authoring`.
- Bounded implementation head: `9c692a284928ad4a28bf859b06b2a54bb74587f9`.
- Final self-referential PR/artifact head: recorded externally in the draft PR and main-chat handoff after this evidence-only commit exists.
- Human/manual evidence: none; every physical/manual condition remains untested.

## Package identity

- Path: `game/data/tales/lantern_house/tale_package_v1.json`.
- Kind/schema/version: `tale` / `1` / `1`.
- Stable Tale ID: `lantern_house_vertical_slice`.
- Canonical SHA-256: `abb39d6bfbdf8d7de108379f08180c13efb99bbffa3e53f30eaaa8de7f459dee`.
- Compatibility: Godot 4.7 family, 1–8 seats, Hidden Betrayer plus cooperative fallback, closed unknown-field policy, repository-authored allowlisted runtime identity.
- Inventory: actions 8; cards 3; connectors 5; Director candidates 8; Director profiles 5; events 3; factions 5; items 2; modes 7; objectives 10; roles 8; spaces 5; stages 5; transitions 10.
- Source ledger: scenario manifest, governed English catalog, and the existing Lantern House board, rules, Director, and social sources. No generated or private source is listed.

## Validator matrix

`python tools/tale_package.py validate` accepts the reviewed package. `python tools/test_tale_package.py` covers 20 unmistakably synthetic cases and rejects unsupported/malformed schema, missing fields, duplicates, unresolved references, invalid and unreachable transitions, unsupported counts, incompatible social/afterlife declarations, missing fallbacks, orphans, invalid privacy, unresolved governed keys, absolute/private paths, URLs, secrets, generated references, unstable ordering, and unstable identity. Diagnostics contain code, source path, and an actionable message. The validator has no network, telemetry, upload, cloud, or AI-service dependency.

## Replay equivalence

The pre-migration authoritative 24-case run at the starting SHA and packaged run share every public-history digest for seeds `4706`, `9017`, and `22031` across 1–8 seats. The named golden fixture covers:

| Case | Seats | Seed | Requested/effective mode | Digest |
| --- | ---: | ---: | --- | --- |
| Normal cooperative | 1 | 4706 | cooperative / cooperative | `5d1707b046dcb2e225e5ee9c35ee592cd4f3d65a15bc619f6446c6edfaf56e53` |
| Accepted fallback | 1 | 9017 | hidden_betrayer / cooperative | `88032c3153f523f5ad4753408b60612420ad8090f0ed666b719cc1ae5de55975` |
| Role/afterlife | 3 | 22031 | hidden_betrayer | `2759b4d201249c54cbf9b56f98f709f51fac1226402fa275cd8ce62ecaf825ae` |
| Ending | 4 | 4706 | hidden_betrayer | `5074f7eb869e7abec6835be9c62a60a3c6c31f0c22a63199e735632b642bc284` |
| Reset/rematch | 2 | 9017 | hidden_betrayer / cooperative | `d49c3ba7e354260ee53fd5779a63bf9f4802e3f80774eaf7c76ad94d901c22f9` |
| Report identity/privacy | 4 | 9017 | hidden_betrayer | `1996aee3cae8894c74da542892d1589036e951862ae92ab8ca17f477806f31e3` |
| Companion/no-phone | 3 | 4706 | hidden_betrayer | `c5dc4fe8146b745f1bbd321fc6c6ff7f270aef65e1a2f2e2af0b0a540c9eb429` |

Stable comparison excludes only build timestamps, evidence presentation, and package provenance digest. Those are non-authoritative; the package digest is absent from snapshots and schema-v2 reports. Save/report schema and stable IDs require no migration.

## Validation ledger

The bounded implementation passed the official Godot 4.7.1 typed import and main-scene smoke; every standalone suite and deterministic simulation; Director 90/90; social 157/157; companion 40/40; vertical slice 24/24; controller-first main-route/report integration; portable identity and bundle tests; pilot schema/privacy/evidence tests; Tale-package positive, negative-fixture, canonicalization, digest, inventory, source-ledger, runtime fail-closed, stable-ID, save/report compatibility, replay-equivalence, and no-network tests; browser-to-service-to-native-to-browser acknowledgement exactly once; GUT 20/20 with 91 assertions and JUnit; Python 3.11.9 hash-locked installation plus `pip check`; `npm ci`, zero-vulnerability audit, strict TypeScript, service 26/26, browser 10/10, Worker/browser builds, and service smoke; repository asset/provenance/privacy/no-network/toolchain/JSON/YAML/secret/size/title/LFS/whitespace gates; and complete first-party `gdlint` and `gdformat --check` over 88 files with zero findings or differences.

The intentional temporary GUT failure-propagation probe exited 1 with one failing test. The probe was removed and the restored suite passed 20/20 with 91 assertions. No probe or generated result is tracked.

The first exact implementation CI attempt passed repository, companion, and Godot workflows but failed portable run `29865151055` because the boot-title smoke sampled coordinator presentation fields before session initialization. The focused correction validates the package directly in the read-only smoke evidence path; the equivalent local native smoke then exited zero with the exact Tale digest and every authority/report/RNG/companion invariance flag true. The correction did not change gameplay initialization or authority state.

The corrected implementation head passed repository run `29865526522`, companion run `29865526595`, Godot run `29865526634`, and portable run `29865526496`. The portable job generated both platforms and passed exact Linux native and launcher smoke.

Warnings and bounded retries: the first local Node command lacked the pinned tool on `PATH`; the pinned Node 24.18.0 installation was then invoked explicitly. Windows execution policy rejected `npm.ps1`; `npm.cmd` ran the same locked commands successfully. npm reported pending optional install-script approvals for esbuild, sharp, and workerd without changing dependencies or failing any build/test. Direct ad hoc background service-launch probes were blocked by desktop command policy; the repository's browser/service/native end-to-end test and CI service smoke passed. The GitHub workflow inspection helper encountered Windows console decoding on Unicode output, so failed logs were inspected directly with authenticated `gh`.

Exact final-head workflow runs, artifact IDs/sizes/digests, outer and inner ZIP CRC results, sidecar hashes, manifest inventories and payload hashes, native/runtime/bundle/Tale identities, Windows local execution, Linux CI execution, and final synchronization state are necessarily recorded externally on draft PR #41 and in the main-chat handoff after the final self-referential head and its artifacts exist. Both platform executions are automated/headless; no physical-controller, television-distance, household, phone, assistive-technology, long-session, or network-environment manual result is claimed.

Issue #39 remains deferred with no branch or PR. PR #32 and issue #7 remain open and untouched. Dependencies, locks, vendored GUT, gameplay content, balance, companions, networking, architecture, and provisional branding remain unchanged.

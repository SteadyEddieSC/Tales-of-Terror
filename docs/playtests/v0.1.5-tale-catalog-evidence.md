# v0.1.5 Tale Catalog & Provider Evidence Ledger

## Fixed boundary

- Starting protected main: `32c43641832aa9fbfedd254ab4f11a92d856c3b4`.
- Branch: `agent/issue-42-tale-catalog-selection`.
- Implementation/artifact-source head: recorded after the bounded implementation commit.
- Final self-referential PR/artifact head: recorded externally in the draft PR and main-chat handoff after the evidence-only commit exists.
- Human/manual evidence: none; every physical/manual condition remains untested.

## Production identity and inventory

- Catalog path/kind/schema/version: `game/data/tales/tale_catalog_v1.json` / `tale_catalog` / `1` / `1`.
- Canonical catalog SHA-256: `2b478fd0d11fa075c2050409193aa06e6b9ca4dcf6efd4e4c550a9f3a5ff9db6`.
- Default and sole production entry: `lantern_house_vertical_slice`.
- Provider: `lantern_house_authorities_v1` version 1, with exact board/rules/Director/social references.
- Package: `tale` / `1` / `1`, SHA-256 `abb39d6bfbdf8d7de108379f08180c13efb99bbffa3e53f30eaaa8de7f459dee`.
- Source roles: governed display, provider registry, and Tale package. Production contains no synthetic, locked, placeholder, coming-soon, or speculative entry.

## Validator and synthetic matrix

`python tools/tale_catalog.py validate` accepts the reviewed catalog and returns its exact default, inventory, compatibility, provider, package, and source ledger. The negative fixture rejects unsupported schema/kind/version, missing and unknown fields, duplicate IDs, invalid defaults, unstable order/identity, absolute/private/test/generated/unresolved/network paths, package kind/schema/version/identity/Tale mismatch, unresolved governed display, unknown/mismatched providers, duplicate/incomplete source roles, secrets, and runtime script/class/evaluation fields.

The excluded two-entry fixture has canonical SHA-256 `06a1f9968fa255aa9bad1cf09fe30ec2553e64599c3f8995d20aaa1610cc31c1`. It proves default Lantern House selection, explicit non-default `synthetic_fixture_tale` selection, stable order and identity, provider rejection, changed package rejection, unknown selection, and atomic retention of the prior valid selection. Every synthetic path is below `game/tests/`; export denylist checks and production-catalog structural tests prevent shipment or normal UI exposure.

## Compatibility and validation ledger

The seven golden replay cases and inherited 24 seed/seat cases retain their exact v0.1.4 semantic digests. Stable IDs, title-to-rematch behavior, 1-8 seats, saves/restoration, snapshots, schema-v2 reports/privacy, public history, RNG streams, companion/no-phone authority, reset/rematch, pilot controls, and portable controls require no migration. Catalog/package provenance remains absent from gameplay snapshots and reports.

Local automated/headless validation passed the official Godot 4.7.1 typed import and main smoke; every inherited standalone suite; Director 90/90, social 157/157, companion 40/40, vertical slice 24/24, seven replay cases, controller-first route/report integration, catalog/package runtime tests, and GUT 21/21 with 96 assertions and JUnit. A temporary intentional GUT test exited 1 with one JUnit failure; the probe was removed and the unchanged suite restored to 21/21. The repository validators passed catalog/package positive and synthetic-negative matrices, deterministic canonicalization/digests/inventories/source ledgers, runtime fail-closed behavior, portable and pilot policies, assets/provenance, privacy, no-network boundaries, toolchain pins, 93-file `gdlint`, 93-file `gdformat --check`, JSON/YAML, LFS, title, secret, size, and whitespace checks.

Python 3.11.9 completed the hash-locked install and `pip check`. Pinned Node 24.18.0 completed `npm ci`, a zero-vulnerability audit, strict TypeScript, service 26/26, browser 10/10, Worker/browser builds, local service smoke, and browser-to-service-to-native-Godot-to-browser acknowledgement exactly once. The first unchanged service-suite attempt had one elapsed-time heartbeat case receive 410 after its compressed 100 ms fixture lifetime; the immediate unchanged rerun passed 26/26. An initial GUT invocation selected an installed Godot 4.7.0 binary and correctly failed only the 4.7.1 patch assertion; all patch-sensitive gates were rerun successfully with the checksum-reviewed 4.7.1 binary. A standalone live-host invocation without its service returned the designed `service_unavailable_without_gameplay_mutation`; the dedicated integrated E2E then passed.

Exact-head CI workflow attempts, artifacts, CRCs, sidecars, manifest payload verification, execution classifications, and synchronization state are recorded after publication. Exact final-head identities are necessarily recorded externally in the draft PR and handoff after this self-referential ledger commit exists.

Issue #39 remains deferred with no branch or PR. PR #32 and issue #7 remain open and untouched. Dependencies, locks, toolchains, vendored GUT, gameplay/content, companions, architecture limits, and provisional branding remain unchanged.

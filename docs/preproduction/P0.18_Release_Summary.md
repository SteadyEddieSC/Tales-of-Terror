# P0.18 — Drowned Harbor High Water Deterministic Transformation Proof

## Bounded result

P0.18 adds one synthetic, test-only, export-excluded lifecycle proof for `DH-FIX-004` / `DH-UI-008` / `DH-IS-008` and the resulting read-only `DH-UI-009` / `DH-IS-009` transformed-board presentation. The fixture inventory remains exactly `DH-FIX-001` through `DH-FIX-007`, and the fixture schema is unchanged.

The authored sources contain no concrete public Lighthouse Council direction, so the bounded public fixture extension uses `synthetic_council_direction_fixture_004`. The release documents that identifier as synthetic fixture data rather than production canon. The extension also provides explicit public before/after hazards, unchanged mechanisms, objective and form continuity, seat positions, legal inspection labels, and persistent changed-category text while retaining every existing private marker.

The adapter validates exact fixture/trace/storyboard identity, system authority, revisions `41` → `42`, stable seat `seat_04`, RNG cursor `12`, event key, once-only marker, route and category completeness, public-form continuity, and private exclusion. It prepares the authoritative result and deterministic event identity before full-versus-skip presentation diverges.

The shell commits once and emits one public `high_water_transformation_committed` event. Deterministic full placeholder presentation and semantic skip converge on byte-equivalent state, revision, event identity/payload, history, transformed board, changed-category summary, caption, transcript, replay, mirror, seat position, public form, inspection inventory, and persistent recap. Duplicate requests and repeated presentation controls reproject without additional outputs.

Pre-commit interruption or projection failure commits nothing. Post-commit caption/voice interruption preserves High Water and moves to the persistent recap. Post-commit projection failure reprojects the existing result rather than recomputing. Transcript/replay unavailability preserves the transformed board. Persistent recap acknowledgement is required before `seat_04` receives focus.

The transformed-board surface uses recognizable Low-Tide placeholder geometry. Labels plus shape/pattern descriptions distinguish open, submerged, flooded-passable, water-only, unstable, damaged, and collapsed route states. All listed actions are read-only inspections; movement, route confirmation, rescue, hazards, encounters, factions, forms, resources, Last Light, endings, balance, final water simulation, final animation/camera, final art/audio, and production authority remain out of scope.

## Isolation and production invariance

The manifest advances completed work to exactly `[80, 81, 82, 83, 84, 85]` and future work to exactly `[86]`. The new test, adapter, shell, scene, and technical authority are explicit test-only registrations. Production registration, export authorization, runtime authority, and human-evidence fields remain false; human validation remains required and unclaimed; issue #39 remains the human-evidence authority.

Drowned Harbor remains development-only, synthetic, explicit-test-script-only, absent from the catalog/provider/Tale Library/saves/reports, without production authority, and excluded from ordinary Windows/Linux exports. Lantern House remains the sole production/default Tale. Catalog, Lantern House, dependency, Companion, Cloudflare, export, portable identity, action-pin, vendored-GUT, issue #39, issue #7, PR #32, and later-roadmap boundaries are unchanged.

## Evidence boundary

Validation is automated/headless. The validator checks the governed fixture, lifecycle source, Godot coverage, workflow boundary, manifest progression, package/catalog digests, dependency pins, export exclusion, and documentation. Its mutation suite reports the actual executed mutation count at runtime; this document does not predeclare a passing count.

The original exact 18-path authorization exposed a compatibility conflict in three inherited feature validators: each still required the exact P0.17 manifest inventory even though its feature-specific checks remained valid. Release Management expanded the boundary to exactly 21 paths so `validate_drowned_harbor_controlled_private_shield.py`, `validate_drowned_harbor_bellhouse_recovery.py`, and `validate_drowned_harbor_low_tide_shell_p016.py` can require the exact P0.18 issue, entry-point, and component inventories. Those compatibility edits preserve every P0.15/P0.16/P0.17 feature assertion and are product-contract progression, not validation suppression.

Focused commands:

```text
python tools/validate_drowned_harbor_high_water_transformation.py
python tools/test_validate_drowned_harbor_high_water_transformation.py
godot --headless --path game --script res://tests/drowned_harbor_high_water_transformation_test.gd
```

No physical-controller validation, television readability, accessibility compliance, privacy/security certification, human comprehension, fun, pacing, fairness, balance, household playtesting, or production-readiness claim is made. Issue #86 remains open and blocked.

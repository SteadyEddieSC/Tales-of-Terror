# P0.8 Codex Return Handoff

**Version:** 1.0
**Repository:** `SteadyEddieSC/Tales-of-Terror`
**Local Codex Desktop path:** `C:\Users\Eddie\Documents\Codex\Tales-of-Terror`
**Prepared from protected main:** `6ea08389f09e5dc6162f63abe65105860b36f22e`
**Expected P0.8 merge:** Record the exact protected-main squash from PR #72 after merge
**Authority:** Remote protected `main` and merged pull requests are authoritative
**Purpose:** Reconcile the local repository, run the actual repository validation suite, and report readiness without authorizing Drowned Harbor runtime implementation

## 1. First action: reconcile, do not develop

When Codex Desktop becomes available, begin with repository reconciliation only.

Do not:

- resume an old local feature branch;
- cherry-pick preproduction commits already merged through PRs #65–#72;
- create a Drowned Harbor runtime package;
- add Drowned Harbor to the Tale catalog or provider;
- replace Lantern House as the sole production Tale;
- generate, purchase, import, or approve production art, SFX, music, voice, fonts, or translated content;
- claim human playtest, television, listening, translation, or accessibility evidence.

## 2. Inspect the local repository before changing it

From the repository root:

```powershell
git status --short --branch
git remote -v
git fetch --prune origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git log --oneline --decorate -12 origin/main
```

Report:

- current local branch;
- local `HEAD`;
- `origin/main`;
- working-tree state;
- untracked files;
- ahead or behind counts;
- local branches whose remote tracking branch no longer exists;
- any local commit not reachable from `origin/main`.

If the working tree is not clean, stop and report the files. Do not discard, stash, reset, or overwrite them without explicit approval.

## 3. Synchronize a clean local main

Only after confirming the tree is clean:

```powershell
git switch main
git pull --ff-only origin main
git status --short --branch
git rev-parse HEAD
git rev-parse origin/main
```

Required result:

- local branch is `main`;
- local `HEAD` equals `origin/main`;
- working tree is clean;
- no merge commit was created;
- no preproduction branch was replayed locally.

Record the exact protected-main squash SHA from PR #72 in the return report.

## 4. Merged preproduction lineage

The local repository should contain these packages:

| Release | PR | Actual scope |
|---|---:|---|
| P0.1 | #65 | Underteller and Drowned Harbor narrative foundation, stable-seat continuity, preproduction roadmap, and AI-media guidance |
| P0.2 | #66 | 104-entry Drowned Harbor dialogue corpus, voice-audition manifest, operating policy, tooling shortlist, bundle procurement, licensing, and original-first visual policy |
| P0.3 | #67 | Visual language, palette guardrails, asset briefs, candidate records, and external review governance |
| P0.4 | #68 | Audio direction and 36 governed ambience, SFX, transition, and system briefs |
| P0.5 | #69 | Music direction, public adaptive-state contract, motifs, stems, and 20 music briefs |
| P0.6 | #70 | Underteller voice direction, casting and rights protocol, and 22 audition-ready line families |
| P0.7 | #71 | Localization, captions, transcript, 22 accessible narrative units, and pseudolocalization |
| P0.8 | #72 | Factual package index, cross-media traceability, validation, and this return handoff |

Use `docs/preproduction/preproduction_package_index_v1.json` as the package ledger after P0.8 merges.

## 5. Run the actual preproduction validators

Use the repository Python version from the repository root.

### P0.2 dialogue and audition corpus

```powershell
python tools/validate_preproduction_dialogue.py
python tools/test_validate_preproduction_dialogue.py
```

P0.1 is documentation-only and does not have a dedicated P0.1 validator.

### P0.3 visual governance

```powershell
python tools/validate_preproduction_visual_assets.py
python tools/test_validate_preproduction_visual_assets.py
python tools/validate_preproduction_visual_candidates.py
python tools/test_validate_preproduction_visual_candidates.py
python tools/validate_preproduction_visual_candidates_reviewed_external.py
python tools/test_validate_preproduction_visual_candidates_reviewed_external.py
```

### P0.4 audio governance

```powershell
python tools/validate_preproduction_audio_assets.py
python tools/test_validate_preproduction_audio_assets.py
```

### P0.5 music governance

```powershell
python tools/validate_preproduction_music_assets.py
python tools/test_validate_preproduction_music_assets.py
```

### P0.6 voice governance

```powershell
python tools/validate_preproduction_voice_lines.py
python tools/test_validate_preproduction_voice_lines.py
```

### P0.7 localization and accessible narrative

```powershell
python tools/validate_accessible_narrative_registry.py
python tools/test_validate_accessible_narrative_registry.py
python tools/test_pseudolocalize_narrative.py
python tools/pseudolocalize_narrative.py --output "$env:TEMP\drowned_harbor_qps_ploc.json"
```

Confirm the generated pseudolocalized package:

- is outside the repository;
- contains 22 families and 88 strings;
- remains visibly marked nonproduction;
- preserves named placeholders;
- leaves the source tree clean.

### P0.8 package and traceability governance

```powershell
python tools/validate_preproduction_package_traceability.py
python tools/test_validate_preproduction_package_traceability.py
```

## 6. Existing engine and repository checks

Run the repository's documented local equivalents for:

- repository formatting and policy checks;
- Godot 4.7 typed parse and import validation;
- first-party GDScript lint and format checks;
- the full existing Godot headless test sequence;
- automated playthrough and deadlock lab;
- portable Windows and Linux build policy where supported locally;
- Companion service and browser tests;
- Companion dependency audit.

Do not suppress the Companion audit.

Issue #44 remains the authoritative remediation item for the inherited `sharp` vulnerability chain. Report the current local dependency tree and audit result without changing lockfiles unless remediation work is explicitly authorized.

## 7. Cross-media traceability review

Review:

- `docs/preproduction/drowned_harbor_cross_media_traceability_v1.json`;
- `docs/preproduction/preproduction_package_index_v1.json`;
- each referenced design, dialogue, and media artifact.

Confirm:

- every referenced path exists;
- every referenced audio, music, voice, and accessible-unit ID exists exactly once;
- voice and accessible narrative remain paired;
- public and private boundaries agree across media;
- stable-seat continuity agrees across design, dialogue, audio, voice, and accessibility;
- High Water, Bellhouse, lighthouse, bargains, forms, endings, and route warnings retain one meaning;
- no package claims production approval;
- no package authorizes a second Tale in the catalog.

## 8. Review local branches without destructive cleanup

```powershell
git branch -vv
git branch --merged main
```

Do not delete a branch containing unmerged local commits. Local branch deletion requires an explicit branch-by-branch review after confirming each branch is fully reachable from protected `main`.

## 9. Working-name and legal boundary

Issue #7 remains open.

Do not make final claims or irreversible changes involving:

- the title `Terror Turn`;
- the host name `The Underteller`;
- storefront names;
- trademarks;
- public domains;
- merchandise;
- public marketing assets;
- credits that imply final brand approval.

Stable internal IDs must not depend on the final legal name.

## 10. Human validation boundary

Issue #39 remains open.

Automation does not establish:

- household playability;
- remote social experience;
- television-distance readability;
- controller comfort;
- caption pacing;
- sound or music fatigue;
- voice clarity;
- translation quality;
- assistive-technology behavior;
- accessibility compliance;
- final visual, audio, music, or narrative quality.

Do not close or reinterpret issue #39 based on preproduction validators.

## 11. No implementation authorization

P0.1–P0.8 form a governed design and preproduction package.

They do not authorize Codex to:

- create `drowned_harbor` under the production Tale package directory;
- modify the Tale catalog or selection provider;
- add Drowned Harbor runtime states, scenes, resources, localization, assets, or tests to the production game;
- create a second playable Tale;
- replace temporary or Lantern House assets;
- select final art, sound, music, voice, fonts, or translations;
- purchase subscriptions, bundles, licenses, or commissions.

A later implementation authorization must name:

- the issue;
- exact scope;
- target branch;
- permitted files or modules;
- acceptance tests;
- asset and licensing policy;
- whether the work is a prototype, vertical slice, or production candidate;
- what remains excluded.

## 12. Allowed local work before implementation authorization

Codex may:

- reconcile and validate the repository;
- report inconsistencies or missing tools;
- improve preproduction validators on a separately authorized branch;
- prepare an implementation plan or issue draft without changing runtime code;
- verify referenced paths and IDs;
- prepare a tool and dependency inventory;
- prepare a bounded candidate-generation or audition plan without generating or purchasing content;
- document human testing prerequisites.

## 13. Required Codex return report

### Repository state

- local branch and `HEAD`;
- `origin/main`;
- working-tree state;
- ahead or behind status;
- stale local branches;
- unmerged local commits.

### Validation state

For every command:

- command;
- exit code;
- pass or fail;
- concise failure excerpt;
- whether the failure reproduces GitHub CI;
- files changed, if any.

### Tool state

- Python version;
- Godot version;
- Node and npm versions;
- GDScript toolkit version;
- browser-test prerequisites;
- unavailable tools or environment blockers.

### Governance state

- Issue #7 remains open;
- Issue #39 remains open;
- Issue #44 audit result;
- Lantern House remains the sole production Tale;
- Drowned Harbor remains design-only;
- no production asset or implementation authorization was inferred.

### Recommendation

Provide one recommended next bounded release or issue. Do not begin it until explicitly authorized.

## 14. Completion condition

The Codex return handoff is complete only when:

- local `main` equals protected `origin/main` after P0.8;
- the tree is clean;
- all applicable local validators have reported results;
- existing engine and repository checks have reported results;
- the Companion audit is reported without suppression;
- no Drowned Harbor runtime implementation occurred;
- the user receives the complete reconciliation report.

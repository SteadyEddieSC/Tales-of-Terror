# P0.19 Release Summary

## Outcome

P0.19 adds the final currently authorized automated evidence and ordinary-export exclusion boundary for the isolated Drowned Harbor prototype. It adds no new gameplay and no runtime prototype component.

The closed `DH-AUTO-P019-V1` profile binds Godot `4.7.1-stable`, the exact prototype manifest, the unchanged seven-fixture package, the four previously implemented feature families, `DH-FIX-005` as projection-only, twelve deterministic aggregate sequences, two repetitions, a 32-step case bound, production identities, export presets, and evidence denials.

## Automated evidence

The aggregate Godot matrix reuses the Low Tide, Bellhouse, controlled-private, and High Water adapters and shells. It compares repeated canonical evidence, exercises cross-family orderings, preserves fixture revisions, stable seats, RNG cursors, event identities and exactly-once outputs, and covers stale revision, wrong authority, wrong stable seat, invalid intent, unavailable surface, replay, duplicate, skip, interruption, disconnect, recovery, and reprojection behavior.

The bounded summary contains public machine evidence only. It reports deterministic equivalence, rejection counts, privacy-leak findings, and deadlock findings without printing private fixture values.

## Packaging boundary

Repository validation proves the two ordinary presets still exclude `tests/*`. Portable automation performs separate native-export and assembled-bundle scans for Windows and Linux using a closed marker inventory derived from test paths, fixture identities, public prototype identities, exact manifest/component/entry filenames, and private sentinels. P0.19 evidence is uploaded separately and does not enter either governed inner portable bundle.

Generated evidence is removed after validation and source-tree cleanliness is required. The production catalog, provider registry, default Tale, project entry point, export presets, launchers, portable identity, and Lantern House digests remain unchanged.

## Evidence limits

This release is deterministic headless automation, not human evidence. It does not establish controller behavior, television readability, accessibility compliance, a privacy or security certification, comprehension, fun, tension, pacing, fairness, balance, household playtesting, remote-device behavior, public-release readiness, or production readiness. Issue #39 remains the human-evidence authority.

Issue #86 is the completed automation-boundary release after merge. No successor Drowned Harbor implementation issue is created or authorized by P0.19.

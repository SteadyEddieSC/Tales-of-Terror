# P0.17 — Drowned Harbor Controlled-Private Shield Proof

## Bounded result

P0.17 adds a synthetic, test-only, export-excluded controlled-private proof for `DH-FIX-003` / `DH-UI-007` / `DH-IS-007` and the exactly authorized `DH-FIX-007` / `DH-UI-016` / `DH-IS-016`. The fixture inventory is exactly `DH-FIX-001` through `DH-FIX-007`.

The proof enters an information-neutral neutral shield before requesting private data, binds the local private surface to the stable seat, controller authority, source and handoff revisions, handoff identity, governed trace, and deterministic counter, and accepts only explicit current-focus acknowledgement. Selecting the governed `refuse_private_bargain` focus with semantic Confirm executes a separate refusal path: it clears every private application layer, commits and emits nothing, advances no result, consumes no RNG, and returns public-safe. B/Escape cancellation/deferral remains separate.

Private and public exactly-once guards are deterministic per handoff/event identity (fixture, handoff/revision, source/result revision, and event key), not lifetime-global aggregate counts. Validation and identity preparation occur before clearing. One shell deterministically completes DH-FIX-003 and then DH-FIX-007, retains both sanitized public history/replay/transcript/mirror entries, rejects true duplicates only, and remains usable after refusal, restoration failure, or post-acknowledgement disconnect.

After a private acknowledgement commits, Cancel no longer clears or fabricates cancellation of the pending sanitized public result. It rejects with `public_restoration_pending`, retains the event identity and sanitized resolution byte-equivalently, and moves or remains in recovery. Post-commit interruption and disconnect likewise use pending-result existence as the discriminator, clear only private application state, preserve the pending result, and move or remain in recovery. Repeated and interleaved Cancel/interruption/disconnect controls emit nothing, change no counts, identity, stable-seat evidence, source/result revision, or RNG evidence, admit no new handoff, and still permit one deterministic public restoration. The neutral post-commit shield and Help text do not advertise destructive cancellation or reveal the private topic, seat, desirability, or result.

The no-phone route stays neutral and supports only governed cancellation, deferral, and Help; it does not expose private terms or commit a default. `DH-FIX-007` preserves the existing stable seat and state without healing, rerolling, restoring inventory, resetting conditions/objectives, erasing history, or changing ending identity.

## Production invariance

Drowned Harbor remains development-only and absent from the production catalog, provider, Tale Library, runtime authority, saves, and ordinary portable exports. Lantern House remains the sole production/default Tale. Package, catalog, dependency, Companion, Cloudflare, action-pin, release-identity, and vendored-GUT files are unchanged.

## Evidence classification

Validation is automated/headless. No human or physical-controller validation, television-readability result, accessibility/privacy/security certification, or fun/pacing/fairness/balance claim is made. Issue #39 remains deferred. Issues #85 and #86 remain blocked.

The focused commands are:

```text
python tools/validate_drowned_harbor_controlled_private_shield.py
python tools/test_validate_drowned_harbor_controlled_private_shield.py
godot --headless --path game --script res://tests/drowned_harbor_controlled_private_shield_test.gd
```

The focused P0.17 validator reports 7 fixtures and 33 embedded fail-closed cases; the correction mutation suite reports 48/48 cases. Exact-head workflow and artifact identities are recorded in the draft PR after GitHub execution; no result is marked passed in advance.

# P0.21 Release Summary — Production Architecture & Tale-Compilation Contract

## Status

Active planning release on branch `docs/p0.21-production-architecture-contract` from protected-main baseline `58f6f4e4ece9bbdd5932216c87aacc064e48e05a`.

## Purpose

P0.21 defines the implementation-ready seam between the existing design-only Drowned Harbor authoring package and future reviewed production Tale authorities.

It does not compile, register, expose, or ship Drowned Harbor.

## Delivered planning authorities

- closed machine-readable production-compilation schema;
- conforming Drowned Harbor compilation contract instance;
- human-readable production architecture and ownership contract;
- blocked implementation issue definitions for alpha.1 through release candidate;
- current-status succession from completed P0.20 to active P0.21;
- fail-closed validators and mutation suites;
- read-only GitHub workflow coverage.

## Core decisions

- authoring references are compilation inputs, never runtime inputs;
- future output authorities remain separate and reviewed;
- `RulesSession` owns stage progression and mutations;
- `BoardState` owns board authority;
- `RoleSession` owns social/private authority;
- the Director receives public and authorized aggregate inputs only;
- presentation remains read-only;
- named native-authority RNG streams own randomness;
- rejected actions are state-and-RNG no-ops;
- restore selects a validated Tale identity before migration;
- migration is explicit and versioned or fails closed;
- package/catalog digests are provenance only;
- future admission is developer-only and explicit until separately promoted;
- Lantern House remains the sole normal production/default Tale.

## Future identities reserved by planning only

- Tale ID: `drowned_harbor`;
- provider ID: `drowned_harbor_authorities_v1`;
- package kind/schema: `tale` / `1`.

No production directory, package, provider registration, catalog entry, normal-library item, save schema, runtime authority, or ordinary export is created.

## Later stages

The following are defined but remain `planned_blocked`, have no GitHub issue, and are not activated:

1. v0.2.0-alpha.1 — Production Tale Scaffold;
2. v0.2.0-alpha.2 — End-to-End Graybox;
3. v0.2.0-alpha.3 — Systems & Replayability;
4. v0.2.0-beta — Presentation & Content Integration;
5. v0.2.0-rc — Hardening & Distribution Readiness.

## Preserved history and boundaries

P0.1–P0.20 remain recorded as merged. P0.20 PR #97 remains preserved at squash SHA `58f6f4e4ece9bbdd5932216c87aacc064e48e05a`.

Issue #44 remains completed. Issue #39 remains the human-evidence authority. Issue #7 remains the naming gate. PR #32 remains unrelated and untouched.

Drowned Harbor remains development-only, explicit-test-script-only, production-unregistered, absent from the normal Tale Library, and excluded from ordinary Windows and Linux exports.

## Evidence boundary

Automation is not human evidence.

P0.21 claims no physical-controller validation, television readability, accessibility certification, privacy certification, security certification, fun, pacing, fairness, balance, production readiness, or public-release authorization.

## Codex routing

Codex is not needed for P0.21. It becomes appropriate only after a separately authorized runtime issue, expected no earlier than v0.2.0-alpha.1.

# v0.1.5 — Tale Catalog, Selection & Runtime Provider Boundary

v0.1.5 places the unchanged Lantern House package behind one deterministic repository catalog, stable-ID pre-session selection, and static reviewed provider registry. The production catalog contains only Lantern House. The generic coordinator no longer names its package or constructs Lantern House content classes.

Catalog validation is offline and fail-closed. Test-only synthetic entries prove stable two-entry ordering, non-default selection, unknown/duplicate rejection, provider/package mismatch rejection, and atomic retention without entering shipped content or player UI.

The release adds no story, board, event, card, item, role, faction, afterlife, ending, art, audio, balance, dependency, toolchain, companion, networking, or branding change. All execution evidence is automated/headless; issue #39 remains deferred.

# Drowned Harbor High Water Deterministic Transformation v1

## Status and authority boundary

P0.18 is a synthetic, test-only, export-excluded technical interaction proof for `DH-FIX-004`, `DH-UI-008`, `DH-IS-008`, `DH-UI-009`, and `DH-IS-009`. It is not a production reducer, complete gameplay system, Tale package, provider, save format, event bus, water simulation, final mechanic, or balance decision. Drowned Harbor remains development-only and absent from the production catalog, provider registry, normal Tale Library, production saves/reports, default startup, and ordinary Windows/Linux exports. Lantern House remains the sole production/default Tale.

The fixture inventory remains exactly `DH-FIX-001` through `DH-FIX-007`; no `DH-FIX-008` exists. The fixture schema is unchanged.

## Authoritative fixture identity

The proof preserves the governed identity:

- fixture `DH-FIX-004`;
- trace `DH-IS-008` and storyboard `DH-UI-008`;
- kind `once_only_public_transform_projection`;
- seed `6108`;
- source revision `41` and result revision `42`;
- RNG cursor `12` before and after;
- actor `system`;
- stable seat `seat_04`;
- intent `project_high_water_transformation`;
- event `high_water_transformation_committed`;
- marker `high_water_committed`.

The authored public sources define the Lighthouse Council stage and commitment trigger but contain no concrete public Council direction. The bounded public fixture extension therefore uses `synthetic_council_direction_fixture_004`. This identifier is explicitly synthetic and is not production story canon.

The open public and seat-public fixture objects add only the proof data needed to express the Council direction, before/after route and hazard categories, an explicit empty unchanged-mechanism set, before/after objective, before/after public form, seat location continuity, fixture-derived read-only inspections, and persistent recap. Existing private sentinels remain unchanged so leak tests retain force.

## Preparation and exactly-once commit

`DrownedHarborHighWaterFixtureAdapter` loads only exact `DH-FIX-004`, validates package inventory and fixture identity, actor authority, revisions, seat identity, request intent, RNG invariance, once-only marker, public category completeness, route identity continuity, form continuity, event classification, and exactly-once status. It rejects missing, malformed, stale, unauthorized, wrong-seat, wrong-fixture, unknown-intent, already-committed, private-tainted, or incomplete inputs without retaining partial fixture state.

Before the presentation path can diverge, the adapter prepares one canonical authoritative after-state, before-state snapshot, changed-category inventory, persistent recap, transformed-board projection, event payload, and SHA-256 event identity. The identity derives only from fixture ID, source revision, result revision, and event key. It uses no wall clock, RNG, object identity, device identity, controller identity, or personal identity.

The shell commits that prepared state exactly once, advances the authoritative revision from `41` to `42`, preserves RNG cursor `12`, retains stable seat `seat_04`, records `high_water_transformation_committed` exactly once, and then presents. Presentation never chooses or modifies the authoritative result.

## Full presentation and semantic skip

The full path uses a deterministic finite three-step placeholder presentation. The semantic skip path changes only presentation progress. Both converge on byte-equivalent authoritative state, result revision, event identity and payload, public history, transformed-board projection, changed-category summary, caption, transcript, replay summary, mirrored output, stable-seat position, public form, legal inspection inventory, and persistent summary.

Repeated skip, acknowledgement, replay, recovery, or duplicate transformation request reuses the existing result. None can consume RNG, transfer authority, replace the seat, change its governed relocation, append a second event/history/transcript/replay/mirror, or emit a second signal.

## Persistent transformed-board recap

Before focus returns, persistent public text identifies:

- committed synthetic Council direction;
- High Water stage;
- Low-Tide and transformed route categories;
- public hazard changes;
- the explicit lack of fixture-declared mechanism changes;
- public objective change;
- unchanged living public form;
- `seat_04` at `salt_market` before and `salt_market_platform` after;
- currently legal read-only transformed-board inspection labels.

The placeholder board retains Bellhouse, causeway, Salt Market platform, lifeboat route, Archive relation, and lighthouse authority as recognizable Low-Tide geography under High Water. Text plus shape/pattern descriptions distinguish open, submerged, flooded-passable, water-only, unstable, damaged, and collapsed states; color is not authoritative.

`DH-UI-009` / `DH-IS-009` is deliberately narrowed to read-only public inspection. The proof may expose geography, route categories, public hazards, stable-seat positions, public forms, the current objective, and fixture-derived action labels. It cannot commit movement, routes, rescue, hazards, encounters, public actions, faction work, Tidebound conversion, Last Light, endings, or any production runtime authority.

## Input, authority, recovery, and deadlock behavior

Only existing semantic actions are used. During presentation, semantic Cancel is presentation skip; Confirm advances the governed lifecycle or acknowledges the persistent recap; Help, transcript, replay, focus navigation, and inspection are presentation-only. Unsupported inputs and every gameplay or authority-transfer attempt fail closed without mutation. The active stable seat becomes the deterministic focus destination only after the recap exists and is acknowledged.

Failure behavior is state-sensitive:

- caption interruption or projection failure before commit records no result or event and exposes a public-safe retry;
- caption or voice interruption after commit preserves High Water and settles the persistent recap;
- post-commit projection failure preserves the committed bytes and recovers by reprojecting, never recomputing;
- transcript or replay unavailability preserves the transformed board and recap;
- stale requests reject without mutation;
- duplicate requests reproject the existing result;
- next interaction remains blocked until persistent summary acknowledgement;
- every recovery mode has a deterministic restoration route and no non-restorable deadlock.

No movement, encounter, rescue, hazard, faction, form, resource, ending, balance, final water, animation, camera, art, or audio reducer runs in this proof.

## Privacy and public evidence

The source fixture deliberately retains `PRIVATE_TIDEBOUND_PENDING`, `PRIVATE_CARRY_NAME_TO_LIGHTHOUSE`, and `PRIVATE_SEAT_04`. Adapter and shell tests prove these values, their fragments, and desirability hints do not enter the public projection, caption, persistent recap, transcript, replay, history, diagnostics, mirror, shared audio, signal, or read-only inspection result.

This is deterministic automated evidence, not privacy or security certification. It is also not physical-controller validation, television-readability evidence, accessibility compliance, human comprehension, fun, pacing, fairness, balance, household playtesting, or production readiness. Human validation remains required and unclaimed; issue #39 remains the human-evidence authority. Issue #86 remains future and blocked.

## Production and dependency invariance

The production Tale catalog SHA-256 remains `2b478fd0d11fa075c2050409193aa06e6b9ca4dcf6efd4e4c550a9f3a5ff9db6`. The Lantern House package SHA-256 remains `abb39d6bfbdf8d7de108379f08180c13efb99bbffa3e53f30eaaa8de7f459dee`. Wrangler `4.114.0`, Miniflare `4.20260722.0`, Sharp `0.35.2`, and `@cloudflare/workers-types` `5.20260722.1` remain unchanged.

P0.18 changes no production Tale code/data, project input map, export preset, Companion source/protocol/service/configuration, Cloudflare behavior, package manifest/lock, dependency version/override, portable identity, action pin, vendored GUT, issue #39, issue #7, PR #32, or later-roadmap work.
